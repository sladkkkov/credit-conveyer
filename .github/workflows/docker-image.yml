name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v1
      - name: set up JDK 17
        uses: actions/setup-java@v1
        with:
          java-version: 1.17
      - name: Maven Package
        run: mvn -B clean package -DskipTests
        
  push_image:
    name: Push image to DockerHub
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD_TOKEN }}
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Docker Build and Push to DockerHub application microservices
        uses: docker/build-push-action@v2
        with:
          context: .
          file: application/Dockerfile
          push: true
          tags: sladkkov/application:latest
          
      - name: Docker Build and Push to DockerHub conveyor microservices
        uses: docker/build-push-action@v2
        with:
          context: .
          file: conveyor/Dockerfile
          push: true
          tags: sladkkov/conveyor:latest
      
      - name: Docker Build and Push to DockerHub deal microservices
        uses: docker/build-push-action@v2
        with:
          context: .
          file: deal/Dockerfile
          push: true
          tags: sladkkov/deal:latest
          
      - name: Docker Build and Push to DockerHub dossier microservices
        uses: docker/build-push-action@v2
        with:
          context: .
          file: dossier/Dockerfile
          push: true
          tags: sladkkov/dossier:latest
          
  
 
